using UnityEngine;

/// <summary>
/// A property attribute to make a field read-only in the inspector.
/// </summary>
public class ReadOnlyAttribute : PropertyAttribute
{
    // This attribute doesn't need any data, it's just a tag.
}